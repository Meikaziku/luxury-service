{% if field.value %}
    <a href="../../{{ field.value }}" target="_blank" class="btn btn-sm btn-primary">
        <i class="fa fa-file-pdf"></i> Voir le CV
    </a>
{% else %}
    <span class="text-muted">Aucun CV</span>
{% endif %}